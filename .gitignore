# Local secrets (T046/T061, FR-017) — never commit. .env.example is the documented template.
.env
.env.local
secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/

# 020 US2 (R6): golden request/response sets are per-machine session artifacts, never fixtures.
tests/goldens/
