'use client';

import { PageTitle } from '@/components/Panel';

// 021 stub — replaced by US4 (T440–T444): policy editor + cycle board + suggestions inbox.
export default function RetrainingPage() {
  return (
    <PageTitle sub="Standing per-model policies close the loop autonomously (coming in this increment).">
      retraining
    </PageTitle>
  );
}
