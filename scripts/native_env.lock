# Native WSL execution environment — exact versions (002 US4, T058 / Principle VI reproducibility).
# These pin the GPU-specific torch-family wheels that requirements.txt intentionally leaves to the
# CUDA index, so a clean machine reproduces the verified environment. Installed into ~/mlops-train
# by scripts/bootstrap.sh. Reference hardware: RTX 5070 Ti Laptop (Blackwell, capability sm_120).
#
# CUDA wheel index for all torch-family packages:
#   --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# The remaining native deps are pinned in the per-component requirements files:
#   training/requirements.txt   -> transformers==4.46.3 peft==0.13.2 accelerate==1.1.1
#                                  datasets==3.1.0 safetensors==0.4.5 sentencepiece==0.2.0
#                                  protobuf==5.29.1 mlflow-skinny==3.14.0 boto3==1.35.99 prefect==3.7.6
#                                  sentence-transformers==3.3.1 optuna==4.9.0
#                                  (012: optuna==4.9.0 added for HPO — pure-Python, server-less, no Ray;
#                                  verified NO torch-family/transformers/peft movement. GPU/FT stack
#                                  stays FROZEN — 012 tunes training hyperparameters, not library pins.)
#                                  (007: mlflow-skinny 2.18.0->3.14.0 [US1]; prefect 3.1.4->3.7.6 [US5].
#                                  010: sentence-transformers==3.3.1 added to training reqs — the
#                                  embeddings fine-tune flow imports it directly; ALREADY in-venv from
#                                  serving/bento (009 embeddings service), so this is a documentation
#                                  pin, not a new install, and reuses the frozen cu128 torch (NO stack
#                                  bump — GPU/FT stack frozen per 010).
#                                  fsspec held at 2026.6.0 — datasets 3.1.0 caps it <=2024.9.0 but a venv
#                                  re-resolve must NOT downgrade it or bentoml 1.4.39 [fsspec>=2025.7.0]
#                                  breaks. torch-family/transformers/datasets FROZEN.)
#   serving/bento/requirements.txt -> bentoml==1.4.39 pillow==12.2.0 sentence-transformers==3.3.1
#                                  lightgbm==4.5.0 joblib==1.4.2  (007 US5: pillow 11.0.0->12.2.0,
#                                  already resolved in-venv via torchvision; bentoml already latest 1.x)
#
# Operator console (003 UI) — Node/UI tier (004 US2, T087). bootstrap.sh enforces a Node floor and
# runs `npm ci` against ui/package-lock.json (the exact JS dep tree is pinned there, not here).
#   node >= 20 LTS        (Node gate in bootstrap.sh; verified on Node 22.x)
#   next == 15.5.19       (patched past CVE-2025-66478; full tree in ui/package-lock.json)
