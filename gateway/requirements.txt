# 007 US3 (T125): pure-Python refresh to current minors on python:3.12-slim. No endpoint/OpenAPI/
# metrics behavior change intended (FR-058).
fastapi==0.138.1
uvicorn[standard]==0.49.0
prometheus-client==0.25.0
httpx==0.28.1
# Pinned explicitly so the pydantic v2 bump is deterministic + its serialization is re-validated (US3).
pydantic==2.13.4
# Registry client only (no server/UI deps) — matches the MLflow server pinned in infra/mlflow.
# 007 US1: 2.18.0 -> 3.14.0 (same version as the server + training client). Gateway FastAPI/boto3/
# Python bumps are deferred to US3 (grill #6) to keep this rebuild a clean MLflow-only bisect.
mlflow-skinny==3.14.0
# S3 client for the MinIO-backed dataset registry. 007 US3: 1.35.99 -> 1.43.36 (independent of the
# mlflow server's boto3 — both are just MinIO clients and need not match).
boto3==1.43.36

# 011 (evaluation gates), T202 / FR-102 / FR-107 — NO new dependency added. The plan floated light
# metric libs (jiwer / sacrebleu / scikit-learn), but every 011 primary metric (LLM task-accuracy,
# vision top-1 accuracy, and the WER / recall@k / AUC / perplexity stubs) is implemented in pure
# Python in app/evaluation.py — the same Principle-III stance monitoring.py took when it computed PSI
# by hand rather than pulling Evidently+pandas+scipy onto the constrained Windows C: drive. sklearn
# would drag in numpy+scipy for math we do exactly in a few lines; jiwer pulls a compiled rapidfuzz.
# Those libs stay swappable behind evaluation.METRICS (Principle V) if a modality later needs them.
