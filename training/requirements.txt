# Native WSL training env (hybrid GPU). torch is installed separately from the CUDA index
# (Blackwell sm_120 → cu128 wheels), so it is intentionally NOT pinned here:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
transformers==4.46.3
peft==0.13.2
accelerate==1.1.1
datasets==3.1.0
safetensors==0.4.5
# Vocab handling for HF->GGUF base conversion (Qwen tokenizer) + its protobuf dep.
sentencepiece==0.2.0
protobuf==5.29.1
# 010 US2 — the embeddings fine-tune flow (flows/embeddings_finetune.py) imports sentence-transformers
# directly. Already installed in the shared ~/mlops-train venv (pinned by serving/bento/requirements.txt
# for the 009 embeddings *service*); pinned here too so the training env is self-describing. MUST match
# the serving pin (same venv); it reuses the frozen cu128 torch, so no new CUDA index / no stack bump.
sentence-transformers==3.3.1
# Tracking + registry client (matches the MLflow server) and S3 for artifact upload.
# 007 US1: 2.18.0 -> 3.14.0 (same version as the server). GPU/FT stack stays frozen; the Prefect bump
# is US5 (T129). torch is the cu128 wheel installed separately and is intentionally untouched here.
mlflow-skinny==3.14.0
boto3==1.35.99
# Flow structure (ephemeral — no Prefect server runs; see training/flows/finetune.py).
# 007 US5: 3.1.4 -> 3.7.6 (current 3.x). Must not perturb the frozen torch/transformers/datasets stack.
prefect==3.7.6
