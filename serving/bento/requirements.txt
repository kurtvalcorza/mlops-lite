# BentoML vision service (T022) — installed into the native WSL training venv (~/mlops-train),
# which already has torch (cu128). torchvision must come from the same CUDA index as torch:
#   pip install torchvision --index-url https://download.pytorch.org/whl/cu128
# 007 US5: bentoml stays 1.4.39 (already the latest 1.x). pillow 11.0.0 -> 12.2.0 — the venv resolver
# already pulled 12.2.0 (a torchvision-side dep) and test_bento is green on it; pin to match reality.
bentoml==1.4.39
pillow==12.2.0
# torchvision (pinned separately, CUDA index): torchvision==0.26.0+cu128
# 009 US2 — embeddings (CPU, off-lease BentoML service). sentence-transformers reuses the torch
# already in the venv (no extra CUDA index needed for CPU inference). 009 US4 — tabular (CPU): lightgbm
# is a light CPU dep, one joblib artifact. Both are additive + swappable (Principle V/III, FR-084).
sentence-transformers==3.3.1
lightgbm==4.5.0
joblib==1.4.2
